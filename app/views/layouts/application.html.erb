<!DOCTYPE html>
<html>
	<head>
  	<%= render 'layouts/info' %>
	</head>
		<body class="parallax">

  		<%= render 'layouts/top' %>

			<%= yield %>

			
		<%= render 'layouts/footer' %>
	</body> 
</html>
